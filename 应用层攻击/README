#web攻击步骤

1. dedecms建站

左侧侧边栏由于权限不足，需要修改权限。将dedecms目录下所有文件设置为777

2. mysql注入

- 利用uploadsafe.inc.php的漏洞，在flink_add.php页面使用Exp.txt中的EXP字符串。

- 在flink.php页面查看图片的源代码，即可获得明文账号和加密密码。
- 根据获取到的账号密码，使用16位MD5解密。注意,dedecms使用20位MD5值，需要去掉前三位和最后一位，获取原始密码。

3. 登入后台，上传木马

可以使用菜刀木马，也可以用其他木马。事实上，菜刀木马已经足够做很多事情

4. 启动菜刀

输入文件路径和变量名。登入会显示遇到错误，原因是文件权限可能无法执行。使用菜刀获取shell，在shell中chmod为777，即可使用菜刀管理网站。